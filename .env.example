# Azure DevOps API version (optional).
ADO_API_VERSION=7.1

# OAuth: client_id in requests is the ADO organization name. Only OAUTH_CLIENT_SECRET is used (optional).
# OAUTH_CLIENT_SECRET=your-client-secret

# Server (Node.js adapter).
PORT=3000

# Local development: full request/response logging (local adapter only, when NODE_ENV != production).
# REQUEST_LOG_PATH=.data/requests.log

# Storage: level (default, local KV), dynamodb (Lambda/production).
STORAGE_TYPE=level
# STORAGE_LEVEL_LOCATION=.data/level
STORAGE_KEY_PREFIX=gitlab-ado-proxy

# DynamoDB (when STORAGE_TYPE=dynamodb):
# STORAGE_TABLE_NAME=gitlab-ado-proxy-storage
# AWS_REGION=us-east-1
