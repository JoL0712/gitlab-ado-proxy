# Azure DevOps Configuration
ADO_BASE_URL=https://dev.azure.com/your-org
ADO_API_VERSION=7.1

# Access Control Configuration (optional)
# Comma-separated list of allowed ADO project names.
# If set, only repositories from these projects will be accessible.
# If not set, all projects are accessible.
# ALLOWED_PROJECTS=Project1,Project2,Another Project

# OAuth Configuration (optional, but recommended for security)
# If set, only requests with matching client_id and client_secret will be allowed
OAUTH_CLIENT_ID=your-client-id
OAUTH_CLIENT_SECRET=your-client-secret

# Server Configuration (optional)
PORT=3000

# Storage Configuration
# Type: memory (default for local dev), dynamodb (for AWS Lambda)
STORAGE_TYPE=memory

# DynamoDB Configuration (only needed when STORAGE_TYPE=dynamodb)
# STORAGE_TABLE_NAME=gitlab-ado-proxy-storage
# AWS_REGION=us-east-1

# Key prefix for namespacing storage keys
STORAGE_KEY_PREFIX=gitlab-ado-proxy
